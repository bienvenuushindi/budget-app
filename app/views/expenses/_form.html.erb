<%= form_with(model: expense, url: category_expenses_path(@category)) do |form| %>
  <% if expense.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(expense.errors.count, "error") %> prohibited this expense from being saved:</h2>

      <ul>
        <% expense.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= form.label :name, class: 'form-label' %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= form.label :amount, class: 'form-label' %>
    <%= form.number_field :amount, class: 'form-control' %>
  </div>
  <div>
    <%= form.collection_check_boxes :category_ids, Category.all, :id, :name %>
    <%#= form.collection_check_boxes :expense, :category_ids, Category.all, :id, :name_with_initial do |item| %>
       <%#= item.check_box %>
    <%# end %>
  </div>
<!--  <div class="form-group">-->
    <%#= form.label :category_id, class: 'form-label' %>
    <%#= form.select :category_id_id, Category.all.collect {|u| [u.name, u.id] },{ :multiple => true, :size => 20 }, class: 'form-select' %>
<!--  </div>-->
  <div>
    <%= form.submit %>
  </div>
<% end %>
